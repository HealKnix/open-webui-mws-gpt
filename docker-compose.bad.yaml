services:
  valkey:
    image: docker.io/valkey/valkey:9-alpine
    container_name: searxng-valkey
    command: valkey-server --save 30 1 --loglevel warning
    restart: unless-stopped
    volumes:
      - ./deployment/valkey-data:/data

  searxng:
    image: docker.io/searxng/searxng:${SEARXNG_VERSION:-latest}
    container_name: searxng-core
    restart: unless-stopped
    env_file:
      - ./.env
    depends_on:
      - valkey
    ports:
      - "${SEARXNG_PORT:-8080}:8080"
    volumes:
      - ./deployment/core-config:/etc/searxng:Z
      - ./deployment/core-data:/var/cache/searxng
  
  open-webui:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_HASH: ${BUILD_HASH:-portable}
    image: openwebui-portable:${WEBUI_DOCKER_TAG:-portable}
    container_name: open-webui
    restart: unless-stopped
    depends_on:
      - ollama
      - searxng
    ports:
      - "${OPEN_WEBUI_PORT:-3000}:8080"
    environment:
      ENV: prod
      PORT: 8080
      OLLAMA_BASE_URL: http://ollama:11434
      SEARXNG_QUERY_URL: http://searxng:8080/search?q=<query>
      WEBUI_URL: http://localhost:${OPEN_WEBUI_PORT:-3000}

      ENABLE_PERSISTENT_CONFIG: "true"
      ENABLE_RAG_WEB_SEARCH: "true"
      RAG_WEB_SEARCH_ENGINE: searxng
      RAG_WEB_SEARCH_RESULT_COUNT: "3"
      RAG_WEB_SEARCH_CONCURRENT_REQUESTS: "10"

      # Базовые настройки
      WEBUI_SECRET_KEY: ${WEBUI_SECRET_KEY:-}
      CORS_ALLOW_ORIGIN: http://localhost:${OPEN_WEBUI_PORT:-3000}
    volumes:
      - ./backend/data:/app/backend/data
    extra_hosts:
      - host.docker.internal:host-gateway

  open-terminal:
    image: ghcr.io/open-webui/open-terminal:latest
    container_name: open-terminal
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      OPEN_TERMINAL_MULTI_USER: "true"
      OPEN_TERMINAL_API_KEY: ${OPEN_TERMINAL_API_KEY:-your-secret-key}
    volumes:
      - ./deployment/open-terminal:/home

  playwright:
    image: mcr.microsoft.com/playwright:v1.58.0-noble
    container_name: playwright
    command: npx -y playwright@1.58.0 run-server --port 3000 --host 0.0.0.0
    ports:
      - "3000:3000"